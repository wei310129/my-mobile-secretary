package com.aproject.aidriven.mymobilesecretary.account.workspace;

/** Identifies the trusted entry point that established a workspace context. */
public enum WorkspaceChannel {
    AUTHENTICATION,
    SYSTEM,
    REST,
    LINE,
    BACKGROUND,
    LOCAL,
    TEST
}
