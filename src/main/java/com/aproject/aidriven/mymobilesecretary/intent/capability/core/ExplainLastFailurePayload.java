package com.aproject.aidriven.mymobilesecretary.intent.capability.core;

/** No arguments are required; the previous failure comes from scoped conversation context. */
public record ExplainLastFailurePayload() {
}
