# 測試環境:資料庫與 Redis 連線由 Testcontainers @ServiceConnection 動態注入,
# 這裡不寫死連線資訊,只放測試專屬的行為設定。
app:
  scheduling:
    # 測試不跑背景輪詢:worker 由測試直接呼叫,確保時序可控
    enabled: false

spring:
  jpa:
    properties:
      hibernate:
        # 測試失敗時方便看實際 SQL
        format_sql: true
